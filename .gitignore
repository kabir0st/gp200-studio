# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env overrides. .env.production is committed on purpose — see its header;
# a GA4 measurement id is public by design and the build needs it.
.env
.env.local
.env.*.local

# Local AI-assistant instructions (kept out of the public repo)
CLAUDE.md
.claude/

# Internal notes and local fixtures (kept out of the public repo)
docs/
mockups/
prst/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
dumps

# Cloudflare Wrangler local state / cache
.wrangler